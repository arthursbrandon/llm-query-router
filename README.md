# llm-query-router
Intelligent query router for LLMs with multi-threading, RAG support, and cost-aware model selection. Routes queries to optimal model (GPT-4, GPT-3.5, Claude, Gemini) based on task type and complexity.
